# Session Log — 2026-04-07

## Objective
Build Equipment Request & Dispatch system from spec.

## Key Findings (Audit)
- Stack is Vite + React Router v7 (NOT Next.js as spec assumed)
- DB tables are `organizations` / `organization_id` (not `companies`)
- Existing `equipment` table (migration 015) stays untouched; new tables added alongside it
- `/equipment` route already exists — expanded with tabs rather than new route
- No job site detail page exists — site-level request view integrated into existing Equipment page
- User FKs in existing migrations use `user_profiles(id)` — followed this pattern

## Work Done

### Migration 022 — equipment_inventory + equipment_requests
- `equipment_inventory`: catalog with qty_total/qty_available, current_job_site_id
- `equipment_requests`: full status flow with requesting/destination site, workflow actors
- RLS: Admin/Manager manage; Supe/Engineer/Foreman insert (own sites only); all org members select (site-scoped)

### Migration 023 — make/model/serial + movement log
- Added `make`, `model`, `serial_number` to `equipment_inventory`
- Created `equipment_movement_log` (immutable, insert-only RLS)

### API — src/lib/api/equipmentRequests.ts
- Full CRUD for both tables
- `transitionEquipmentRequest`: status transitions with dispatch side-effects (inventory update, equipment table sync, movement log write)
- `fetchMovementLog`: per-item history

### UI Components (all new)
- `EquipmentRequestForm` — submit form with inventory dropdown + free-text fallback
- `EquipmentRequestCard` — expandable card with status badge, make/model/serial, movement line
- `DispatchModal` — captures make/model/serial (eq # required) + dispatch notes
- `EquipmentInventoryForm` — add/edit catalog items
- `EquipmentInventoryCard` — shows make/model/serial + availability + location
- `EquipmentRequestsTab` — role-adaptive (admin sees all + actions; Supe/Foreman see site only)
- `EquipmentInventoryTab` — catalog management with per-item movement history
- `MovementHistory` — lazy-loaded history timeline per inventory item

### Equipment.tsx — expanded
- Added 3-tab layout: Equipment | Requests | Inventory
- Admin with no site selected: fetches all org equipment with job_sites join, groups by site
- Non-admin: unchanged (site-scoped or prompt to select)

### EquipmentCard.tsx
- Added `showSite` prop (used in admin all-sites view)

## Commits
| Hash | Description |
|------|-------------|
| 3b75d40 | Add equipment request & dispatch system |
| aa54b6f | Add make/model/serial, movement log, dispatch sync |
| b04ac83 | Admin Equipment tab shows all company equipment grouped by site |
| 6f549c3 | Capture make/model/serial during dispatch |
| aab45e9 | Make Equipment # required before dispatching |

## Build Status
✅ All commits built clean (zero TS errors)

## Open TODOs (deferred)
- Push/email notifications on status change
- QR scan to mark received
- Attach equipment to crew/task
- Partial dispatch quantity refinement
