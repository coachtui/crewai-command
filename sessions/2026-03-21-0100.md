# Development Session - 2026-03-21-0100

## Phase
Admin All-Sites Access + Multi-Site Worker Tracking Completeness

## Objectives
- Give admin role full visibility into all org job sites (same as manager)
- Ensure multi-site worker tracking (daily hours, roster, task assignment) is complete
- Build passes clean

## Starting Context
- Current phase: Bootstrap complete, first real feature session
- Known blocker(s): Admin role scoped to assigned sites only
- Files reviewed: JobSiteContext.tsx, roleHelpers.ts, DailyHours.tsx (lines 1-300)
- Assumptions:
  - Multi-site worker queries (daily hours, roster, task assignment modal) were already implemented in recent commits
  - Only the admin site-visibility scoping was missing

## Actions Taken

**Session start** - Read JobSiteContext.tsx and roleHelpers.ts fully
- Confirmed: `fetchJobSites` only fetched all sites for `isManager` — admins fell into the else branch (assigned sites only)
- Confirmed: `getUserSiteRole` only skipped role lookup for `isManager` — admins queried DB unnecessarily
- Confirmed: `canViewAllSites` in getPermissions was `userIsManager` only
- Confirmed: multi-site worker pattern (primary + worker_site_assignments) already in place in DailyHours, Workers roster, AssignmentModal

**Change 1** - JobSiteContext.tsx: fetchJobSites
- Old: `if (isManager) { // fetch all sites }`
- New: `if (isManager || isAdmin) { // fetch all sites }`
- Admins now load all org job sites into availableJobSites

**Change 2** - JobSiteContext.tsx: getUserSiteRole
- Old: `if (isManager) return null;`
- New: `if (isManager || isAdmin) return null;`
- Admins skip the site-role DB query; all their permissions derive from isAdmin already

**Change 3** - roleHelpers.ts: getPermissions
- Old: `canViewAllSites: userIsManager`
- New: `canViewAllSites: userIsManagerOrAdmin`
- Permissions object now correctly reflects admin's all-sites access

## Validation
- Tests run: N/A (no test suite)
- Lint/typecheck: `npm run build` — ✅ PASSED, zero TS errors
- Manual verification: Build output confirmed, CSS warnings are pre-existing (Tailwind arbitrary values)
- Outstanding risks: None

## Files Modified
### Modified
- src/contexts/JobSiteContext.tsx (lines 43, 105)
- src/lib/roleHelpers.ts (line 294)

## Decisions / Observations
- Decision: Treated admin as "same level as manager" for site visibility. Admin can now see all org sites and switch between them via the site selector. All edit/manage actions already gated by `isAdmin` on individual features.
- Observation: The job site selector (useShouldShowJobSiteSelector) already returns true when `availableJobSites.length > 1`, so admins will automatically see the site switcher once they have all sites loaded.
- Observation: Daily hours, Workers roster, and Assignment Modal already correctly include temp-assigned (multi-site) workers via the worker_site_assignments pattern.

## Next Session
- Immediate next step: User smoke test — verify admin sees all sites in the site switcher
- Blockers: None
- Whether CTO/architect input is needed: No
